<div class="banner">
    {% set banniere = Image(page.meta('banniere')) %}
    {% set banniere_url = banniere.src %}

    <div class="single-banner with-overlay" style="background-image:url('{{banniere_url|resize(1920,400) }}')">
        {% if page.contenu_banniere %}
        <div class="row content">
            <div class="col-12 banner-overlay">
                {{page.contenu_banniere}}
            </div>
        </div>
        {% endif %}
    </div>
</div>